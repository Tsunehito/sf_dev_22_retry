{% extends 'base.html.twig' %}

{% block title %}Hello Sf Dev22{% endblock %}

{% block body %}
    <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
        <h1 class="display-4">Sf Dev22</h1>
        <p class="lead">Quickly build an effective pricing table for your potential customers with this Bootstrap example. It’s built with default Bootstrap components and utilities with little customization.</p>
    </div>
    <div> 
    <div class="card-deck mb-3 text-center">
        {% for article in articles %}
            <div class="card mb-4 box-sm">
                <div class="card-header">
                    <h4 class="my-0 font-weight-normal">{{ article.title}}</h4>
                </div>
                <div class="card-body">
                    <div class="mt-3 mb-4">
                    {{ article.content|slice(0, 50)}}
                    </div>
                    <a href="{{ path('article_show', {'id': article.id}) }}" class="btn btn-lg btn-block btn-outline-primary">
                        Read more
                    </a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
